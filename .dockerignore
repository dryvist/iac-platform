# The semaphore build (compose/semaphore/Dockerfile) uses the repo root as
# its build context so it can COPY scripts/ — keep VCS metadata and worktrees
# out of what gets sent to the docker daemon.
.git
.worktrees
